import React from "react";

function Comment(props) {
  return (
    <div>
      {/* Your JSX code for the component goes here
     - show already existing comments as a list or Map
     - Create a text box, where you can write a comment
     - Add a button that "adds" the text to the list of comments */}
    </div>
  );
}

export default Comment;
